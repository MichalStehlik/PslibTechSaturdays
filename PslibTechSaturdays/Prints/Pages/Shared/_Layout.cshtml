<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Diplom Template">
    <title>Osvědčení</title>
    <style>
        @@page {
            size: A4 landscape;
            margin: 1cm;
        }
        
        @@media print {
            body {
                margin: 0;
                padding: 0;
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                font-size: 10pt;
            }
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
            background-color: #f5f5f5;
        }

        .certificate {
            width: 297mm;
            height: 210mm;
            background: url('/image/certificate_bg.png') no-repeat center;
            background-size: cover;
            border: 2px solid #333;
            padding: 20mm;
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        .certificate h1 {
            font-size: 24pt;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .certificate p {
            margin: 5px 0;
        }

        .signature-block {
            margin-top: 30mm;
            width: 100%;
            display: flex;
            justify-content: space-between;
            padding: 0 20mm;
        }

        .signature-block div {
            width: 30%;
            text-align: center;
            border-top: 1px solid #000;
            padding-top: 5px;
        }
    </style>
</head>
<body>
    <div class="certificate">
        @RenderBody()
    </div>
</body>
</html>
